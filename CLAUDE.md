# 作業メモ（island maker）

リポジトリ・手元のフォルダ・公開先（Cloudflare）の名前はすべて `island-maker`（旧名 hakoniwa）。

**つまみで島をつくり、鳥になって自分の島を飛び、URL を送って友達と一緒に飛ぶ。**
目的も期限もない。作った島を見せたくなることが芯。

`.claude/` があれば、コードを触る前に読むこと（方向性と経緯。git には入れていない）。
前身は `../stroll`（無限に広がる世界を飛ぶ）。地形の作り直しを検討した結果、
有限の島を作る別プロジェクトとして始めた（2026-09-24）。

## 設計の芯

- **島は有限。** 島全体を 1 枚の高さの格子として一度に計算する（`src/island/grid.ts`）。
  無限の世界では 1 点ずつの関数に縛られて侵食も川も作れなかったが、有限なら
  「周りを見る計算」（水を満たす・流れを集める・雨陰）を普通に書ける
- **島は URL で渡す。** つまみと種は `#` の後ろに載る（`src/island/params.ts`）。
  サーバに島を保存しない。同じ URL なら誰が開いても同じ島
- **川は流れを先に決め、地形を川に合わせる**（`src/island/hydrology.ts`）。
  海から水を満たして湖を決め、流れを集めて川にし、川の水面は下流へ必ず下がる

## 決定性の決まり（静かに壊れる）

**生成には四則演算・`Math.sqrt`・`Math.floor`/`round`・`Math.min`/`max` だけを使う。**
`Math.sin` / `exp` / `pow` / `log` などは JavaScript エンジンごとに結果がわずかに違いうる。
侵食のような繰り返しの計算は差を増幅するので、同じ URL でも iPhone と PC で別の島になる。
表示（`src/view/`）はこの決まりの外。

- 同じ高さの並べ替えは番号で決着させる（`MinHeap`、流れの順序）。並びを入力だけで決める
- ノイズは格子の番号ではなく正規化座標（-1..1）で引く。下見と本番で同じ形になるように
- 乱数は種から作った `mulberry32` だけ。`Math.random` を生成に使わない

## 構成

```
src/island/   島全体の計算（決定性の決まりの内側）
  params.ts     つまみと URL
  grid.ts       島の大きさ（4km）・格子・近傍・ヒープ
  landscape.ts  隆起させた山を川が削る（FastScape 型の侵食、16m 格子）
  hydrology.ts  湖と川（水を満たし、流れを集める）
  forest.ts     島全体の木（近くのチャンクと同じ規則・乱数）
  overviewArrays.ts  見渡す島の 1 枚の中身（Worker で作る）
  generate.ts   工程をつなぐ入口 / worker.ts  Worker で計算する
src/world/    1 点ずつ引く地形と気候（stroll から持ってきたもの＋島用）
  islandShape.ts  大きな形の 3 次補間＋細部 / islandWater.ts  水の格子を引く
  terrain.ts      公開窓口（描画・植生・プレイヤーはこれだけを見る）
  chunk.ts・scatter.ts・climate.ts・surfaceShade.ts ほか  stroll の資産
src/render/   見渡す島と遠景（overviewMesh.ts）、遠目の木（farForest.ts）、
              近くのチャンク（chunkManager.ts）、空・水・木の形（stroll の資産）
```

- つまみを動かしている間は粗い格子（侵食 129・見渡す島 257）で下見し、
  離したら本番（侵食 257＝16m・見渡す島 769＝約 5m）で作り直し、木も植える
- 飛んでいる間、近くはチャンク（足元 2m・本物の木）、遠くは島全体の 1 枚と遠目の木。
  チャンクのできている所を coverage テクスチャで知らせ、重なりも穴も出さない
- stroll の標高の式で作る平らで広いワールドは、タグ `flat-world-v1`（ブランチ `flat-world`）に残してある

## stroll から引き継いだ教訓（該当する部分を足すときに読む）

- smoothstep の最大傾斜は平均の 1.5 倍。「高さ H を幅 W で落とす」は 1.5×H/W になる
- 水面とほぼ同じ高さの平らな岸は、深度の精度が足りずチラつく。見渡すカメラは数 km 離れるので、
  near を距離に合わせて上げる（main.ts の fitNearPlane）。飛んでいる間も高度に合わせて上げる
- **海の板を巨大な三角形にしない・カメラに少しずつ付けて動かさない。** 80km の三角形 2 枚では
  深度の補間誤差が大きく、しかも毎フレームずれるので、動いている間だけ水際がガタついた
  （止まると直った）。400m 四方に分け、追従も 400m 刻みにする（render/water.ts）。
  確かめ方: 時間を止め、カメラを 0.0005rad ずつ回した画面の差を見る（水際に線が出たら再発）
- **水面に polygonOffset（深度を奥へずらす補正）を使わない。** ずらし量は斜めから見るほど大きく、
  広い海の板では数 m ずれて、浅瀬の海底が水面を突き抜けてちらついた（島を縁取る砂色の帯に見えた）。
  水面のシェーダーが自分の下の水深を知っているので、水深 0 に近づくほど透明にして描き合いの帯を消す
- **`reversedDepthBuffer` を使わない。** three r185 で有効にすると、遠くの陸の上に海の板が
  かぶって島の奥半分が白く覆われた。polygonOffset の向きを直しても消えず、原因は未特定
- 湖の底を海面より下げない。海の板が湖の中に見えて水面が二重になる
- スマホ GPU の `mediump` で大きな値を `fract()` に入れない（空の雲に亀裂が出た）
- iOS Safari は click の pointerType を誤って報告する。直前の pointerdown を見る
- 再接続でアバターの死体が残る。接続 ID を sessionStorage に固定して送る

## 地面と水の描き方（ここで決めたこと）

- **地面の色は面や頂点の単位で混ぜ切らない。** 頂点は土台・岩・雪の 3 層と量を持ち
  （world/islandSurface.ts）、境目は画素ごとにノイズで揺らして切る（render/terrainMaterial.ts）。
  面ごとに 1 色だった頃は、雪と岩の境が格子に揃って市松模様になった（遠くの粗いチャンクほど大きい）
- **画素ごとの模様はシェーダーで計算せず、起動時に作ったテクスチャから引く。**
  波（render/waveTexture.ts）も地面の質感（render/noiseTexture.ts）も。測ると手続きノイズの方が重く、
  テクスチャならミップマップが遠くの細かすぎる模様を均してくれる（ちらつき・点の格子が出ない）
- **波を少数の正弦波の和で作らない。** 周期が揃って、遠くで斜めの格子、近くで網目の模様になった。
  波はスペクトルから逆フーリエ変換で 1 枚作り、比が整数でない 3 つの大きさで引く。
  遠くの照り返しは、均されて消えた波の傾きの分散で広げる（LEAN、Bruneton 2010）
- **崖の模様は真上からだけ貼らない。** 斜面に沿って縦に伸び、毛皮のような筋になった。3 方向から貼る
- **岸の泡は水深でなく、水際からの距離で置く**（水深 ÷ 海底の傾き）。水深で決めると遠浅の浜で
  帯が数十 m に広がり、砂浜に白い影が染み出した。泡は海だけ。川と湖に立てると白い土手に見えた
- **水のシェーダーの水深は、川に合わせて彫った後の高さで測る**（main.ts の setHeightMap）
- **雪は陸の上だけ。** 気温は海の底でも寒い地方では低く、海底が白くなった
- **鳥は水に潜らない**（player/controller.ts の keepAboveGround）。潜ると外洋の段差や海底が見える
- **光は島ごとに 1 度だけ焼き込む**（island/lighting.ts → render/islandLight.ts）。太陽の影と空の見え方を
  高さの格子で測り、描くときはテクスチャを 1 回引くだけ。島は有限で太陽は動かないからできる。
  計算（本番で約 0.3s）は島を送った後に Worker が続けて行い、届いたら影を浮かび上がらせる。
  影は暗くするだけでなく空の青い光を足す（直接光を消すだけだと、低い朝日で島の半分が濁った）
- **規則的な縞を入れない。** 岩の地層を高さの sin で入れたら、斜面に等間隔の横縞が並んで模様に見えた

## 速さ（開いてから島が見えるまで）

- **見せるのに要る物から順に送る**（`island/worker.ts`）。島＋見渡す島の 1 枚＋地図 → 木 → 光。
  見渡す島の 1 枚（59 万頂点の色付け、約 1 秒）は Worker で作る（`island/overviewArrays.ts`）。
  画面側で作ると、その間 画面が止まっていた
- **開いたら先に粗い下見を出し、続けて本番の細かさで作り直す**（`main.ts` の最後）
- **侵食の隣は番号の差で引く**（`landscape.ts` の `nOff`）。`for (const [di, dj, dd] of NEIGHBORS8)` の
  分解だけで侵食全体の 8 割（1.3 秒）を使っていた。書き方を変えても計算の順番と値は同じなので島は変わらない
- 測った値（M3）: 島が見えるまで 0.6 秒、入れるまで 1.7 秒（直す前は両方 2.9 秒）。stroll は約 0.8 秒で入れる。
  差の根は作り方の違い（stroll は足元の 9 区画だけ、island maker は島全体の侵食・川・気候を先に計算する）

## 画面と操作（stroll と揃える）

- **見た目と操作は stroll と同じにする**（`src/ui/overlay.ts`・`src/ui/touch.ts`・`player/controller.ts`）。
  ガラスのカード・丸いボタン・折りたたみの操作説明、Pointer Lock とタッチの切り替え、
  iOS Safari の入力の誤報への備え（`resolveEntryPointerType`）、Esc で休憩、最初の 15 秒の操作ガイド、
  速度と高度の表示、AUTO、速さで広がる視野、高度で薄くなる霧。stroll 側を直したらこちらも揃える
- カードは島を隠さない（PC は左、スマホは下）。見渡すときはカメラの中心をずらして、
  カードの外の空いた所の真ん中に島を映す（`main.ts` の `frameIsland`）。入口のボタンはカードの下に固定
- 休憩すると見渡す画面に戻り、つまみを触れる。続きは休憩した場所から飛ぶ
- サイコロは合言葉もつまみも全部振り直す（端の極端な島を避けて 10〜90）

## コマンド

```bash
npm run dev   # http://localhost:5173（LAN 公開）
npm run ci    # 型・ビルド（テストはまだ無い）
npm run deploy  # ビルドして Cloudflare へ（https://island-maker.hochu.workers.dev）。静的ファイルだけ
```
